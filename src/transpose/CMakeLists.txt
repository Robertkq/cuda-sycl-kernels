
if(CMAKE_CUDA_COMPILER)
  add_executable(transpose-naive-cuda cuda/naive.cpp)
  set_source_files_properties(cuda/naive.cpp PROPERTIES LANGUAGE CUDA)
  set_target_properties(transpose-naive-cuda PROPERTIES LINKER_LANGUAGE CUDA)
  target_link_libraries(transpose-naive-cuda PUBLIC benchmark)
  target_include_directories(transpose-naive-cuda PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/../common)
endif()

if(CMAKE_CUDA_COMPILER)
  add_executable(transpose-optimized-cuda cuda/optimized.cpp)
  set_source_files_properties(cuda/optimized.cpp PROPERTIES LANGUAGE CUDA)
  set_target_properties(transpose-optimized-cuda PROPERTIES LINKER_LANGUAGE CUDA)
  target_link_libraries(transpose-optimized-cuda PUBLIC benchmark)
  target_include_directories(transpose-optimized-cuda PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/../common)
endif()

if(SYCL_CUDA_AVAILABLE)
  add_executable(transpose-naive-sycl sycl/naive.cpp)
  target_compile_options(transpose-naive-sycl PRIVATE ${SYCL_CUDA_FLAGS})
  target_link_options(transpose-naive-sycl PRIVATE ${SYCL_CUDA_FLAGS} -Wl,--disable-new-dtags)
  target_link_libraries(transpose-naive-sycl PUBLIC benchmark)
endif()

if(SYCL_CUDA_AVAILABLE)
  add_executable(transpose-optimized-sycl sycl/optimized.cpp)
  target_compile_options(transpose-optimized-sycl PRIVATE ${SYCL_CUDA_FLAGS})
  target_link_options(transpose-optimized-sycl PRIVATE ${SYCL_CUDA_FLAGS} -Wl,--disable-new-dtags)
  target_link_libraries(transpose-optimized-sycl PUBLIC benchmark)
endif()
