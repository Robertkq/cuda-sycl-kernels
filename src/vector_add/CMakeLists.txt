
if(CMAKE_CUDA_COMPILER)
  add_executable(vector_add-naive-cuda cuda/naive.cpp)
  set_source_files_properties(cuda/naive.cpp PROPERTIES LANGUAGE CUDA)
  set_target_properties(vector_add-naive-cuda PROPERTIES LINKER_LANGUAGE CUDA)
  target_link_libraries(vector_add-naive-cuda PUBLIC benchmark)
  target_include_directories(vector_add-naive-cuda PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/../common)
endif()

if(CMAKE_CUDA_COMPILER)
  add_executable(vector_add-optimized-cuda cuda/optimized.cpp)
  set_source_files_properties(cuda/optimized.cpp PROPERTIES LANGUAGE CUDA)
  set_target_properties(vector_add-optimized-cuda PROPERTIES LINKER_LANGUAGE CUDA)
  target_link_libraries(vector_add-optimized-cuda PUBLIC benchmark)
  target_include_directories(vector_add-optimized-cuda PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/../common)
endif()

if(SYCL_CUDA_AVAILABLE)
  add_executable(vector_add-naive-sycl sycl/naive.cpp)
  target_compile_options(vector_add-naive-sycl PRIVATE ${SYCL_CUDA_FLAGS})
  target_link_options(vector_add-naive-sycl PRIVATE ${SYCL_CUDA_FLAGS} -Wl,--disable-new-dtags)
  target_link_libraries(vector_add-naive-sycl PUBLIC benchmark)
endif()

if(SYCL_CUDA_AVAILABLE)
  add_executable(vector_add-optimized-sycl sycl/optimized.cpp)
  target_compile_options(vector_add-optimized-sycl PRIVATE ${SYCL_CUDA_FLAGS})
  target_link_options(vector_add-optimized-sycl PRIVATE ${SYCL_CUDA_FLAGS} -Wl,--disable-new-dtags)
  target_link_libraries(vector_add-optimized-sycl PUBLIC benchmark)
endif()
